import React from 'react';

interface {{pascalCase featureName}}PageProps {
    // defina as props, se necessário
}

function {{pascalCase featureName}}Page(props: {{pascalCase featureName}}PageProps) {
    return (
        <div>
            <h1>{{pascalCase featureName}} Page</h1>
        </div>
    );
}

export default {{pascalCase featureName}}Page;