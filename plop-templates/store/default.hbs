import { createSlice } from '@reduxjs/toolkit';

const initialState = {
    // Estado inicial
};

export const {{camelCase featureName}}Slice = createSlice({
    name: '{{camelCase featureName}}',
    initialState,
    reducers: {
        // Defina os reducers
    },
});

export const { /* ações */ } = {{camelCase featureName}}Slice.actions;
export default {{camelCase featureName}}Slice.reducer;